{
  "name": "whist-game",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --filter @whist/server dev",
    "build": "pnpm --filter @whist/shared build && pnpm --filter @whist/client build && pnpm --filter @whist/server build",
    "test": "pnpm --filter @whist/server test",
    "seed": "pnpm --filter @whist/server seed",
    "migrate": "pnpm --filter @whist/server migrate"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
